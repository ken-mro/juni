# FLICK IMPACT — 仕様書

フリック入力で降ってくるひらがなの隕石を破壊するタイピングゲーム。旧称「十二 JŪNI」（12キーに由来）。改名後もリポジトリ名・`localStorage` のキー（`juni.*`）は変えない。公開URLは `flick-impact.com`。**1プレイ = 1レベル**。モードは2つ: **チャレンジ**は60秒以内にそのレベルの規定数を撃破すればクリアで、成績は**クリアにかかった時間**（短いほど良い）。**スコアアタック**は浮遊する隕石を60秒間にいくつ壊せるかで、成績は**撃破数**（多いほど良い）。

## 制約

- **単一HTMLファイル** (`index.html`)。CSS・JSすべてインライン。
- 外部依存ゼロ。CDN、npm、ビルドツール、フレームワークすべて不使用。
- 配信先は Cloudflare Workers（`https://flick-impact.com`）。`index.html` を静的アセットとして配信し、Google ログインとプレイデータ保存の API だけを同一オリジンの Worker（`worker/index.js`）が担う。バックエンドが無い配信先でも `index.html` 単体で従来どおり動く。
- 対象はスマートフォンのタッチ操作。デスクトップはマウスでデバッグできれば十分。
- `localStorage` は使用可（記録・設定の保存）。

## クラウド同期（Google ログイン・任意）

- タイトル画面の下部に、枠と塗りのあるボタン「Googleでログインする」と補足「記録と設定を保存して、別の端末でも引き継げます」を置く。ログイン中は表示名・同期状態（同期中… / 同期済み / 同期できませんでした）・ログアウトを表示する。バックエンドが応答しない配信先では欄ごと非表示。
- ログインは `/auth/google/start` への画面遷移（Authorization Code + PKCE。トークン交換は Worker 側）。HTML には Google のスクリプトを読み込まない。取得するのは表示名のみ（スコープ `openid profile`）。
- セッションは HMAC 署名付き HttpOnly Cookie（90日）。ログアウトしても手元の記録は消さない。
- 保存単位はユーザー（Google の `sub`）ごとの `{ records, scores, settings, updatedAt }`（`records` = チャレンジ、`scores` = スコアアタック）。起動時にサーバーと手元を統合し、以後は記録・設定が変わるたびに少し待ってまとめて送る。画面を閉じる直前の変更も送る。
- 統合規則（端末側・サーバー側で同一）: チャレンジの記録はレベルごとに**タイムが短いほう**、スコアアタックの記録はレベルごとに**撃破数が多いほう**、設定は**更新が新しいほう**。複数端末で遊んでも記録は失われない。`scores` を知らない古いクライアントからの保存でもサーバー側の `scores` は残す。プレイ中はサーバーの設定を反映しない。

## 画面構成

縦画面固定レイアウト。上から順に:

1. **HUD** — 左: ロゴ + レベル（固定）のピル / 中央: 残り時間（大きく） / 右: 撃破 `k / 規定`（進行ゲージ付き）・コンボ。スコアアタックでは撃破数だけを出し、ゲージはそのレベルのベスト撃破数に対する進み（初挑戦なら空のまま）
2. **フィールド** — 隕石が上から落下するエリア（画面の上部 60〜65%）。スコアアタックでは隕石が落ちずにここで浮遊する
3. **キーパッド** — 3×4 の12キーグリッド（画面下部）

配色は「ビビッドな宇宙」。深い紫〜藍の空に黄・シアン・ピンクのネオン系アクセント。空の色はレベルごとに決まり、高いレベルほど暖色。

### タイトル・ロゴ・アイコン

- **ロゴ**は「FLICK」「IMPACT」の2段組をレトロゲーム風のドット文字で描く。外部フォントは使わず、ファイル内のドットフォント（高さ7・2ドット幅の太字。英大文字・数字・`!` `.` 空白）から生成する。DOM（HUD左上・タイトル画面）は SVG、共有画像は canvas に、同じデータから描く。塗りは黄→ピンク→シアンのグラデーション＋右下へ1ドットの影。タイトル画面では光彩を足す。
- **英字の見出し**（リザルトの CLEAR! / TIME UP、RANK とランク文字、共有画像の LEVEL n CLEAR）も同じドット文字で描く。日本語の見出し（一時停止・設定・記録・画像を保存）は明朝体のまま。
- **アイコン**（favicon）は `<head>` に data URI で埋め込む SVG。左下の地球へ「あ」の乗った隕石が炎の尾を引いて落ちてくる図。隕石の配色（紫の岩・ラベンダーの縁・ピンクの光）はゲーム内と揃える。

## 入力仕様

### キー配置

3列×4行の12キー。`あ か さ / た な は / ま や ら / 小゛゜ わ 、`

各キーは中央＋上下左右の5方向で1行分のかなを入力する。

- 中央（フリックせず離す）= あ段
- 左 = い段 / 上 = う段 / 右 = え段 / 下 = お段

`や` 行は や・ゆ・よ のみ、`わ` 行は わ・を・ん のみ。存在しない方向は無反応にする。

### 実装方針

- **Pointer Events で統一する**（`pointerdown` / `pointermove` / `pointerup`）。`touchstart` は使わない。マウスで同じコードパスを通せるようにしておくこと。
- `pointerdown` でキーを掴み、`pointerup` 時点の移動量で方向を確定する。
- 方向確定のしきい値は **25px**。定数 `FLICK_THRESHOLD` として先頭に置く。しきい値未満は中央（あ段）扱い。
- 方向判定は `Math.atan2` ではなく `Math.abs(dx) > Math.abs(dy)` の比較で四方向に落とす。斜めの取りこぼしを防ぐ。
- `<body>` に `touch-action: none;` と `overscroll-behavior: none;` を設定。これがないとモバイルで画面が引っ張られてゲームにならない。スクロールさせたい要素（オーバーレイのパネル、開始レベル選択の枠、記録一覧）には `touch-action: pan-y` を個別に指定する。
- 高さは `100dvh` を使う（`100vh` はモバイルのアドレスバーで破綻する）。
- `user-select: none;` でキーの長押し選択を無効化。

### 入力ガイド

`pointerdown` した瞬間、押したキーの上下左右にかなをポップアップ表示する（IMEと同じ挙動）。設定でON/OFF切り替え可能にし、デフォルトはON。

## 単語データ

### 素データ形式

```js
const WORDS = [
  "ねこ",
  "そら",
  "たいよう",
  "すいせい|彗星",
  "りゅうせいぐん|流星群",
];
```

- 読みだけの文字列。漢字表記を出したい場合のみ `よみ|表記` のパイプ区切り。
- 日常語を軸に、宇宙・天体系の語彙を2〜3割混ぜる。約600語。
- **かなの網羅**を保つ: 清音はすべて5回以上出現し、語頭にも3語以上立つ。濁音・半濁音は3回以上出現し、ぢ・づ以外は語頭にも立つ。小文字(ゎ・ぅ以外)は3回以上。単語のレベル別プール(L6 以降)は L6 ≥ 130 / L7 ≥ 220 / L8 ≥ 320 / L9 ≥ 400 語を下限とし、**語頭の種類**が L2 以降で 22 以上あること(出題規則の前提。L1〜L5 はかな単体なので語頭 = その字)。
- 読みは `METEOR_MAX_LEN`（7）文字以内。語頭に ん・を・ー・小文字は置かない。
- ファイル冒頭付近の `const` としてベタ書き。外部JSON化はしない。

### 起動時の導出

単語ごとの難易度メタは手でタグ付けせず、起動時に計算する。

```js
const COST = { dakuten: 1, handakuten: 2, small: 1 };

function analyze(kana) {
  // → { actions, rows, flags, len }
}
```

- `toBase(ch)` — が→か、ぱ→は、ゃ→や の正規化
- `rowOf(base)` — 所属する行（"か" など）を返す
- `extraCost(ch)` — 清音0、濁音1、半濁音2、小文字1
- `actions` — 実際のフリック回数の合計。**難易度の基準はこの値**。文字数ではない
- `rows` — 使用する行の配列。出題フィルタに使う
- `flags` — 濁音/半濁音/小文字/長音のビットフラグ

### バリデーション

起動時に全単語を検査し、問題があれば `console.warn` に単語と理由を出す。ゲーム自体は該当語をスキップして続行する。

弾く対象: カタカナ、漢字（読み側）、半角スペース、想定外の記号、空文字、`|` が2つ以上、読みの重複、`METEOR_MAX_LEN` 超え、語頭が ん・を・ー・小文字。

あわせて `console.info` で「一度も出現しないかな」と「語頭に立たない清音（ん・を・ー を除く）」を報告する（メンテナンスの目安。ゲームには影響しない）。

## ゲームルール

### モード

タイトル画面のモード切替（チャレンジ / スコアアタック）で選ぶ。選択は設定 `mode` に保存し、あそびかた・開始レベル選択・背後の HUD がモードに従う。

| | チャレンジ | スコアアタック |
|---|---|---|
| 隕石 | 上から落ちてくる。着地で撃破数 −1・揺れ・危険ライン | **落ちずに浮遊**し、壊されるまで残る。着地・減点・危険ライン・揺れは無い |
| 同時数 | `METEOR_MIN`〜`METEOR_MAX`（出現間隔で補充） | **最大2個**（`SCORE_METEORS`）。上下2つの「席」に1体ずつ。撃破から `SCORE_RESPAWN` 秒で補充 |
| 終了 | 規定数到達でクリア／時間切れ | **時間切れのみ**（失敗は無い） |
| 成績 | クリアタイム（短いほど良い）＋ランク | 撃破数（多いほど良い）。ランク無し |
| レベル | 1〜（上限なし）。クリアで次が解放 | **1〜10**。最初から全部選べる |
| 記録 | `juni.records`（レベルごとのベストタイム） | `juni.scores`（レベルごとのベスト撃破数） |
| 隠しステージ | 5の倍数のクリアから入れる | 無し |

以下の「1プレイ = 1レベル」「規定撃破数」「隕石」の項はチャレンジの規則。スコアアタックの差分は「スコアアタック」の項にまとめる。

### 1プレイ = 1レベル

- 開始時に選んだレベルで**固定**。プレイ中にレベルは変わらない。
- 制限時間は **60秒**（`TIME_LIMIT`）。時間内に**規定撃破数**に達すれば**クリア**でゲーム終了。達しなければ時間切れで終了（記録は残らない）。
- 残り10秒（`LAST_SPURT`）からラストスパート: タイマーが赤く点滅し、秒ごとにカウント音、BGMが加速する。
- クリアするとそのレベルの**次のレベルが解放**される。クリア画面の「次のレベルへ」で即座に次を開始できる。

### 規定撃破数

```
出現間隔(level) = max(SPAWN_INTERVAL × 出現間隔係数(level), SPAWN_INTERVAL_MIN)
規定数(level)   = max(1, round(TIME_LIMIT ÷ 出現間隔(level) × CLEAR_RATIO))
```

1分間の出現予定数に係数 `CLEAR_RATIO`（初期0.8）をかけた数。難度の調整は出現間隔と係数で行う。

### 隕石

- 1隕石 = 1単語（2〜4文字）。
- 表示は読みのかな。次に入力すべき文字をハイライトし、残りを淡く表示する。
- 濁音・半濁音・小文字の途中（「ば」で「は」まで打った状態など）は、清音を暗く出し、まだ打っていない記号だけを明るく重ねる（濁点は ゛、半濁点は ゜、小文字は「小」バッジ）。小゛゜をあと2回押す「ぱ」「づ」の1回目には「×2」を添える。何を押せばよいかが一目で分かるようにするため。
- 正しいかなを入力すると先頭から1文字削れ、隕石にヒビが入る。全文字入力で破壊。撃破した語の表記（漢字）が浮かび上がる。
- **隕石が最下部（キーパッド上端）に到達すると撃破数が1戻る**（0未満にはならない）。あわせてコンボが切れ、画面が揺れ、`-1` が表示される。
- 場の隕石が `METEOR_MIN` 未満なら出現間隔を待たずに補充する。速く撃破すればそのぶん早くクリアできる。

### ターゲティング

プレイヤーに狙いを選ばせない。**入力されたかなが先頭文字と一致する隕石のうち、最も下にあるものを自動で対象にする。**

一致する隕石がなければミスとして扱い、コンボをリセットする。

**ロック解除**: ミスタイプで狙っていない隕石にロックしてしまったときのために、ロックは自分で外せる。ロック中はフィールド左下に「ロック解除」ボタン（照準と同じシアン）が出て、これを押すかフィールドをタップするとロックが外れる（軽い解除音）。外した隕石に入った文字はそのまま残り、次の入力は通常どおり「一致する隕石のうち最も下」を狙う。コンボは切れない。

### 出題規則

同じような語が続かないよう、候補を段階的に絞って一様に選ぶ。

1. **語頭のひらがなが直近 `RECENT_FIRST_KANA`（20）語に出ていない**（が と か は別の文字として扱う）∧ 先頭の入力トークンが場の隕石と重複しない ∧ 同じ語が直近 `RECENT_WORDS`（20）語に出ていない
2. 同じ語の条件を外す
3. 語頭の条件も外す（場内の先頭トークン重複だけは最後まで避ける — ターゲティングの前提）
4. 完全ランダム

語頭を避ける語数の上限は、そのレベルのプールにある**語頭の種類数 − `FIRST_KANA_SLACK`（2）**。あ・か・さ行しかない L1 は語頭（= かな）が15種のため 13 語連続、L2 以降は 20 語連続で語頭が重複しない。

### レベル別の難易度

落下速度ではなく**行の解放**で難易度を上げる。L1〜L5 は**かな1字**の隕石だけで、落下速度・出現間隔は据え置き（行と字種の解放だけで難しくなる）。L6 から単語になり、速度が上がって出現間隔が縮む。

| レベル | 解放される行 | 文字数 | 追加要素 |
|---|---|---|---|
| 1 | あ か さ | 1 | 清音のみ |
| 2 | ＋ た な は | 1 | |
| 3 | ＋ ま や ら わ | 1 | 全清音 |
| 4 | 全行 | 1 | 濁音・半濁音 |
| 5 | 全行 | 1 | ＋小文字（単体） |
| 6 | 全行 | 2 | 単語になる。速度・出現頻度が上がり始める |
| 7〜10 | 全行 | 2〜3 … 2〜6 | 最大文字数が1つずつ増える |
| 11〜 | 全行 | 制限なし | 速度と出現頻度が上がり続ける（規定数も増える） |

1字の出題は `FLICK_MAP` のかなから作る（ん・を・ー・ゔ・ゎ は出さない。小゛゜で作れない字 = `CYCLES` に無い字は単語と同じ検査で除外する）。出題可能な単語は `w.rows.every(r => unlocked.has(r))` かつ現在レベルで許可されたフラグのみを含むもの。

### スコアアタック

- **浮遊**: 隕石は落ちてこない。フィールドの上下2つの席（`FLOAT_SLOTS` = フィールド高さの 30% / 62%）のどちらか空いているほうに現れ、席の中心のまわりを漂う（横 `FLOAT_BOB_X`・縦 `FLOAT_BOB_Y` の揺れ。`FLOAT_POP` 秒かけて膨らんで出る）。席を上下に分けるのは、7文字の語（幅≈190px）が横に2つ並ぶと狭い端末で重なるため。壊されるまでずっと残る。
- **補充**: 場の隕石が `SCORE_METEORS`（2）未満なら補充する。最初の1体は即、以後は撃破から `SCORE_RESPAWN`（0.35秒）おいて出る。
- **平和ではない**: ビーム・破片・撃破語の表記・コンボ演出はチャレンジと同じ。無いのは着地（と減点・揺れ・危険ライン）だけ。
- **終了**: 60秒の時間切れだけ。見出しは「FINISH!」（ドット文字・黄→シアン）で、TIME UP の失敗色は使わない。
- **レベル**: 1〜10。難しさは字種と文字数だけで決まり（落下が無いので速度・出現間隔の概念が無い）、語彙はチャレンジのプールをそのまま借りる（`SCORE_LEVELS[i].base`）。

  | スコアアタック | 借りるチャレンジのプール | 内容 |
  |---|---|---|
  | 1 | L1 | あ・か・さ（1字） |
  | 2 | L2 | ＋た・な・は（1字） |
  | 3 | L3 | ＋ま・や・ら・わ（1字） |
  | 4 | L5 | 濁音・半濁音・小文字（1字。チャレンジの L4・L5 をまとめて解放） |
  | 5 | L6 | 全字種・2文字 |
  | 6〜9 | L7〜L10 | 全字種・2〜3 … 2〜6文字 |
  | 10 | L11 | 全字種・2〜7文字（制限なし） |

- **開始レベル**: 10レベルすべて最初から選べる（クリアの概念が無いので解放は無い）。各マスにはベスト撃破数を併記し、「最高クリア」の行は出さない。選択は設定 `scoreLevel` に保存する。
- **記録**: レベルごとのベスト撃破数（撃破数が上回ったときだけ丸ごと置き換え）を `juni.scores` に保存する。`juni.records` には触れない（チャレンジの解放判定に影響しない）。
- **BGM**: 専用曲（下記「音」）。テンポはレベルで変えない。空の色はチャレンジと同じ表をスコアアタックのレベル番号 1〜10 で引く。

### 開始レベル選択

タイトル画面でモードと開始レベルを選べる。チャレンジの解放上限は **クリア済み最高レベル + 1**（上限なし）。選択肢は最低 `START_LEVEL_SHOWN`（10）個を並べ、解放がそれを超えたぶんだけ増える。表示枠は2段分の高さに固定し、超えたぶんは枠内をスクロールする（スクロールバーは出さない。マスの幅はスクロールの有無で変えない）。各マスにはそのレベルのベストタイムを小さく併記する。未解放はロック表示。選択は設定として保存する。枠の下に選択中レベルの説明（解放行 ／ 文字数 ／ 規定数。レベル番号は付けない）を出す。スコアアタックの違いは上記のとおり（10マス固定・全解放・ベスト撃破数・規定数なし）。

### スタート直後の READY → GO!

スタート（およびリザルトの「次のレベルへ」「もう一度」）を押すと、フィールド中央にドット文字（`PIXEL_FONT`）で **READY**（`READY_TIME` 秒）→ **GO!**（`GO_TIME` 秒）を出してからゲームを動かす。この間は隕石を出さず、入力も受けず、タイマーも進まない。設定画面を開いている間は進行を止める。GO! はゲームが動き出したあと拡大しながら消え、BGM はこのタイミングで鳴り始める。

### コンボ

- 正解入力ごとに +1。ミス入力・着地でリセット。
- `COMBO_MILESTONE` 連鎖ごとにバナーとチャイムで祝う。HUDのコンボ数字は増えるたびに弾む。

## リザルト画面

1つのオーバーレイを複数のモードで共用する。

- **クリア**: 見出し「CLEAR!」（ドット文字・黄→ピンク）。主役は**クリアタイム**（小数2桁）とランク（`RANKS`: クリアタイムのしきい値で S/A/B/C）。ベストタイム更新（初クリア含む）なら「記録更新」バッジ＋紙吹雪＋ジングル。レベル / ベストタイム / 最大コンボ。行別正答率（↑↓はそのレベルの前ベスト比）・方向別ミス率。ボタン: **次のレベルへ**（▶| アイコン） / もう一度（↻ アイコン。同レベルを即再開。主ボタンの下に枠付きの控えめな塗り） / 画像を共有・保存 / タイトルへ（5の倍数のレベルでは「次のレベルへ」の上に隠しステージの「?」）
- **時間切れ**: 見出し「TIME UP」（ドット文字・ラベンダー）。主役は `撃破 / 規定`。ランクなし。ボタン: **もう一度**（↻ アイコン。同レベルを即再開。ここでは主ボタン）/ タイトルへ
- **記録の閲覧**: クリアと同じ表示で、ボタンは 画像を共有・保存 / 閉じる
- **スコアアタック**: 見出し「FINISH!」（ドット文字・黄→シアン）。主役は**撃破数**（個）。ベスト撃破数更新（初プレイ含む）なら「記録更新」バッジ＋紙吹雪＋ジングル。レベル / ベスト / 最大コンボ。ランク無し。ボタン: **次のレベルへ**（L10 では出ない） / もう一度（次のレベルへが出るときは控えめ、L10 では主ボタン） / 画像を共有・保存 / タイトルへ。「?」は出ない
- **スコアアタックの記録の閲覧**: スコアアタックと同じ表示で、ボタンは 画像を共有・保存 / 閉じる
- **隠しステージ**（下記）: 見出し「THANK YOU」（ドット文字・シアン→ピンク）。主役は**友好度**（%）。ベスト友好度更新なら「記録更新」バッジ＋紙吹雪＋ジングル。ランク・レベル・共有は無し。ボタン: **次のレベルへ**（▶| アイコン。入ったレベルの次の通常レベルを開始）/ もう一度（↻ アイコン。説明を挟まず隠しステージを再開）/ タイトルへ

**行ごとの入力正誤率**と**方向ごとの誤入力率**（「下フリックが弱い」といった癖の可視化）は、このゲームの練習ツールとしての核なので全モードで表示する。

### 隠しステージ「ファーストコンタクト」

- **入口**: 5の倍数のレベル（`SECRET_EVERY`）をクリアしたリザルトにだけ、既存のボタンに加えて「?」ボタンが出る。押すと説明パネル（見出し「FIRST CONTACT」・あそびかた・**スタート** / タイトルへ）が出て、「スタート」で隠しステージが始まる（タイトルからは入れない。再挑戦も5の倍数のクリアから）
- **内容**: 空の色が変わり、上部に UFO が漂う。ドームの中の宇宙人が「ふきだし」のことば（そのレベルの語彙）を降らせるので、フリックで打ち返す。1文字ごとに火花、1語打ち終えるとハートが UFO へ飛んで**友好度**（撃破数 / 規定数。規定数はそのレベルと同じ）が上がる。宇宙人の表情は友好度で 無表情 → にっこり → 大喜び → 100% で目が星 と変わる
- **平和的**: 撃たない・壊さない・減点しない。ふきだしが下端に着いても「…？」と消えてコンボが切れるだけ。危険ラインも揺れも無い
- **終了**: 60秒（通常と同じ）か友好度 100% で終了。**失敗は無い**。落下速度・出現間隔・規定数はそのレベルの隕石と同じ（`SECRET_SPEED` は 1.0）。BGM は専用曲（下記）
- **記録**: 入ったレベルごとのベスト友好度だけを `juni.settings.secretBests[level]` に保存する（`juni.records` には触れない。開始レベルの解放にも影響しない）。記録一覧ではそのレベルの直後に「？？？ 友好度 NN%」の行を出す（タップしても何も出ない）。クラウド同期ではレベルごとに高いほうを取る
- **宇宙人の色**: 入ったレベルごとに肌の色が変わる（L5 は緑、以降 `ALIEN_SKINS` の順に L10, L15, …）

## 記録

- タイトルの「記録を見る」で、「チャレンジ」（クリア済みレベルの LEVEL / ベストタイム / ランク。隠しステージの「？？？」行を含む）と「スコアアタック」（LEVEL / ベスト撃破数）のグループに分けて一覧を表示し、タップするとそのときのプレー結果を閲覧できる（記録のあるモードだけ見出しを出す）。
- **SNS共有画像**: プレー結果を 1080×1080 の PNG に描く（空のグラデーション・ロゴ・LEVEL n CLEAR・クリアタイム・RANK・最大コンボ・行別正答率・キャッチコピー・サイトURL。ロゴ・LEVEL n CLEAR・RANK はドット文字）。スコアアタックは見出しが「SCORE ATTACK / LEVEL n」の2段、添え書き「60秒間の撃破数」、主役が撃破数（個）で、RANK は無い。共有テキストとファイル名は `FLICK IMPACT` / `flick-impact-level{n}.png`（スコアアタックは `flick-impact-score-level{n}.png`）。Web Share API が使える端末では共有シートを開き、使えなければ画像を表示して長押し保存とダウンロードリンクを提供する。

### 保存（localStorage）

- `juni.records` — `{ [level]: { level, time, maxCombo, destroyed, required, rows, dirs, t } }`。チャレンジのレベルごとのベストタイムと、そのときの統計（ok/ng）。開始レベルの解放判定にも使う
- `juni.scores` — `{ [level]: { mode: "score", level, score, maxCombo, rows, dirs, t } }`。スコアアタックのレベルごとのベスト撃破数と、そのときの統計。解放判定には使わない
- `juni.settings` — `{ guide, hint, sfx, bgm, startLevel, secretBests, mode, scoreLevel }`（`secretBests` は `{ [レベル]: ベスト友好度 }`。未プレイのレベルは無い。`mode` は `"challenge"` / `"score"`、`scoreLevel` はスコアアタックの開始レベル 1〜10。範囲外は読み込み時と同期の統合時に既定へ戻す）
- 旧キー `juni.best` / `juni.highscore` / `juni.history` は読まない

## 音

- 音源ファイルは使わず Web Audio API で合成する。AudioContext は初回のユーザー操作で起動する。
- BGMは**チップチューン風ポップ**（Cメジャー、BPM132前後、25%パルス波リード＋三角波ベース＋チップコード＋ノイズ打楽器）。レベルが高いほど、またラストスパートでテンポが上がる。
- 隠しステージの BGM は**専用曲**（3拍子のワルツ。C | F | G | C | Am | F | G | C、BPM172 固定 `SECRET_BPM`）。宇宙人とくるくる踊るような賑やかさ: 1拍目にキック+ベース（ブン）、2・3拍目に和音の刻みとタンバリン（チャッ・チャッ）、8分で走る高いアルペジオ、8分ハット、跳ね回るパルス波リードにビブラート。
- スコアアタックの BGM も**専用曲**（通常曲と同じ明るさの F メジャー。F | Dm | Bb | C | F | Am7 | Gm | C7→F の8小節（最後の小節は後半で F に戻る）、BPM140 固定 `SCORE_BPM`。レベルでは変えず、ラストスパートの加速だけ通常と同じ）。通常曲との違いは**軽いシャッフル**（裏の8分を拍の 0.62 の位置に置く `swing`）: 根音→5度→オクターブと歩く跳ねるベース、小節頭の薄いパッドと2・4拍の和音のバックビート、キック1・3拍・スネア2・4拍・跳ねた8分のハット、折り返しのスネアの助走。16分のチップコードは使わない。
- 隠しステージの「?」を押したときは専用の効果音（宇宙人の呼びかけのようなブリップ → 上昇するうねり → きらめき）。
- 効果音は明るい方向に統一する: 正解入力は長音階を登る、破壊は上昇チャイム（隠しステージでは爆発の無い柔らかいアルペジオ）、クリアは上昇して終わるファンファーレ、記録更新はきらめき。時間切れは控えめに下がる2音、着地は落ちる2音（警報にはしない）。

## 実装の順序

各フェーズの終わりに必ずブラウザで動く状態にすること。

1. レイアウトの骨組み＋12キーグリッドの描画
2. フリック入力の判定ロジック（押したかなを画面に表示するだけのデバッグ表示）
3. 語彙モジュール（`analyze` / `toBase` / `rowOf` / `extraCost` / バリデーション）
4. 隕石の落下・描画・ターゲティング
5. コンボ、時間制限、規定数とクリア判定、着地ペナルティ
6. レベル別の難易度、開始レベル選択と解放
7. リザルト画面（3モード）と統計、記録の保存・閲覧、共有画像
8. 入力ガイドのポップアップ、設定のON/OFF

## 描画方針

隕石のフィールドは `<canvas>`、キーパッドとHUDはDOM。キーパッドをcanvasで描くとタップ領域の管理が煩雑になるので分離する。

`requestAnimationFrame` で駆動し、フレーム間の経過時間 `dt` で移動量を計算する（フレームレート依存にしない）。

## コードスタイル

- 調整対象の数値（しきい値、制限時間、規定数の係数、落下速度、出現間隔、ランクしきい値、コスト）はすべてファイル先頭の定数ブロック（`CONFIG`）にまとめる。canvas の描画色は `PALETTE`、DOM の色は CSS `:root` に集約する。
- 実機で調整することになるので、マジックナンバーを関数内に埋め込まない。
